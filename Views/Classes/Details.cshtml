@model Learning_Space.DTO.ClassDTO

@{
    ViewData["Title"] = "Details";
}
@using Microsoft.AspNetCore.Http
@{
    int? user = Convert.ToInt32(Context.Request.Query["user"]);
    int? permission = Convert.ToInt32(Context.Request.Query["permission"]);
    int? classid = Convert.ToInt32(Context.Request.Query["classid"]);
}
<div class="general">
<h1>Details</h1>

<div>
    <h4>Class</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ClassName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ClassName)
        </dd>
    </dl>
    <p> @Model.Students Stusents.</p>
</div>
<div>
        <a asp-controller="Courses" asp-action="Index" asp-route-user="0" asp-route-permission="0" asp-route-classid="@Model?.ClassId" class="options">courses list</a>
        <a asp-controller="Users" asp-action="Index" asp-route-user="0" asp-route-permission="0" asp-route-classid="@Model?.ClassId" class="options">students list</a>
</div>
<div>   
        <a asp-action="Edit" asp-route-user="0" asp-route-permission="0" asp-route-classid="@Model?.ClassId" class="options">Edit</a>
        <a asp-action="Index" asp-route-user="0" asp-route-permission="0" class="options">Back to List</a>
</div>
</div>