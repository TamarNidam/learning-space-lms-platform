@model Learning_Space.DTO.LessonDTO
@if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
{
    <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
}
@{
    ViewData["Title"] = "Create";
}
@using Microsoft.AspNetCore.Http
@{
    int? user = Convert.ToInt32(Context.Request.Query["user"]);
    int? permission = Convert.ToInt32(Context.Request.Query["permission"]);
    int? courseid = Convert.ToInt32(Context.Request.Query["courseid"]);
}

<h1>Create</h1>

<h4>Lesson</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" asp-route-user="@user" asp-route-permission="@permission" asp-route-courseid="@courseid">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <div class="form-group">
                <label asp-for="LessonSubject" class="control-label"></label>
                <input asp-for="LessonSubject" class="form-control" />
                <span asp-validation-for="LessonSubject" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="LessonDate" class="control-label"></label>
                <input type="date"  asp-for="LessonDate" class="form-control" required />
                <span asp-validation-for="LessonDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="StartTime" class="control-label"></label>
                <input id="startTimeInput"  min="08:00" max="22:00" step="3600" asp-for="StartTime" class="form-control" />
                <span asp-validation-for="StartTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EndTime" class="control-label"></label>
                <input id="endTimeInput" @* type="time" *@ min="09:00" max="23:00" step="3600" asp-for="EndTime" class="form-control" />
                <span asp-validation-for="EndTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="LessonType" class="control-label"></label>
                <select asp-for="LessonType" name="LessonType" class="form-control" id="LessonType">
                    <option value="Frontal">Frontal</option>
                    <option value="Zoom">Zoom</option>
                    
                </select>            
                <span asp-validation-for="LessonType" class="text-danger"></span>
            </div>
            <div class="form-group" id="zoomUrlContainer" style="display:none;">
                <label asp-for="ZoomUrl" class="control-label"></label>
                <input asp-for="ZoomUrl" class="form-control" />
                <span asp-validation-for="ZoomUrl" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="sign_up_button" />
            </div>
        </form>
    </div>
</div>

<div>
   @*  <a asp-action="Index">Back to List</a> *@
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        
        
        // var startTimeInput = document.getElementById("startTimeInput");
        // var endTimeInput = document.getElementById("endTimeInput");
        var zoomUrlContainer = document.getElementById("zoomUrlContainer");
        var lessonTypeSelect = document.getElementById("LessonType");


        // // Validate end time
        // startTimeInput.addEventListener("change", validateEndTime);
        // endTimeInput.addEventListener("change", validateEndTime);

        // function validateEndTime() {
        //     var startTime = startTimeInput.value;
        //     var endTime = endTimeInput.value;

        //     if (startTime && endTime) {
        //                 var startDateTime = new Date("2000-01-01 " + startTime);
        //         var endDateTime = new Date("2000-01-01 " + endTime);
        //         if (endDateTime <= startDateTime) {
        //             endTimeInput.setCustomValidity("End time must be greater than start time.");
        //         } else {
        //             endTimeInput.setCustomValidity("");
        //         }
        //     } else {
        //         endTimeInput.setCustomValidity("");
        //     }
        // }

        // Show/hide zoomUrlContainer on Lesson Type change
        lessonTypeSelect.addEventListener("change", function () {
            if (lessonTypeSelect.value === "Zoom") {
                zoomUrlContainer.style.display = "block";
            } else {
                zoomUrlContainer.style.display = "none";
            }
        });
    </script>
}
